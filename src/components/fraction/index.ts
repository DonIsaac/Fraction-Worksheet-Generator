export {
    FractionComponent,
    FractionInput
} from "./Fraction"

// Types are exported seperately because of --isolatedModules flag
export type {
    FractionComponentProps,
    FractionInputProps
} from "./Fraction"
